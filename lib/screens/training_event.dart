import 'package:flutter/material.dart';

class training_event extends StatelessWidget {
  const training_event({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("training_event"),
    );
  }
}
